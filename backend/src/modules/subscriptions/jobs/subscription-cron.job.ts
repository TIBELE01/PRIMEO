// Subscription cron job (module-level) — delegates to the global job registry
// The actual implementation is in src/jobs/subscription.job.ts
export { startSubscriptionJob } from '../../../jobs/subscription.job';
