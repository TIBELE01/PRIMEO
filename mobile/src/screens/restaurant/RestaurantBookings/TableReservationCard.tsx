import React from 'react'; import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';
export const TableReservationCard: React.FC<{ reservation: any; onAccept: () => void; onDecline: () => void }> = ({ reservation, onAccept, onDecline }) => (
  <View style={styles.card}><Text style={styles.guest}>{reservation.guestName} — {reservation.guestCount} pers.</Text><Text style={styles.time}>{reservation.reservationTime}</Text><View style={styles.btns}><TouchableOpacity style={styles.d} onPress={onDecline}><Text style={styles.dt}>Refuser</Text></TouchableOpacity><TouchableOpacity style={styles.a} onPress={onAccept}><Text style={styles.at}>Confirmer</Text></TouchableOpacity></View></View>
);
const styles = StyleSheet.create({ card: { backgroundColor: '#fff', borderRadius: 12, padding: 14, marginBottom: 10, elevation: 1 }, guest: { fontWeight: '700', fontSize: 14, marginBottom: 2 }, time: { color: '#777', fontSize: 13, marginBottom: 10 }, btns: { flexDirection: 'row', gap: 10 }, d: { flex: 1, borderWidth: 1, borderColor: '#B71C1C', borderRadius: 8, padding: 8, alignItems: 'center' }, dt: { color: '#B71C1C', fontWeight: '600' }, a: { flex: 1, backgroundColor: '#1056E0', borderRadius: 8, padding: 8, alignItems: 'center' }, at: { color: '#fff', fontWeight: '700' } });
