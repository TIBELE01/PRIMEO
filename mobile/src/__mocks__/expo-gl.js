// Stub for expo-gl — VirtualTourScreen requires EAS Development Build, not Expo Go.
// Metro resolves this file in dev so the bundle compiles; the screen shows a
// "dev build required" message at runtime instead of crashing on import.
module.exports = {};
